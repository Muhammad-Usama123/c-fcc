// # include <iostream>

// int main(){
//     auto result = (10 <=> 20) > 0; // "<=>" is called spaceship operator for testing compiler supports our given (in this case c++ 20) version or not.
//     std::cout << result << std::endl;
// }

// Prefix And PostFix + And - : // A cool thing that we can do with addition and subtraction....

    // int value {5};

        // Increment by one in variable value:

            // value = value + 1; // 6

        // Increment by one in variable value using Postfix:

            // value = value++; // it will first print 5 then in next turn 6.

        // Increment by one in variable value using Prefix:

            // value = ++value; // it will first increment and then print 6 in the first time...

        // value = 5; // reset the vaLue to 5

        // Decrement by one in variable value:

            // value = value - 1; // 4

        // Decrement by one in variable value using Postfix:

            // value = value-- ; // it will first print 5 then in next turn 4.

        // Decrement by one in variable value using Prefix:

            // value = --value ; // it will first decrement and then print 4 in the first time...

    // We can increment and decrement by value more than 1 like let's say 5 or 7, we'll see that later in this course Insha Allah...

        
            