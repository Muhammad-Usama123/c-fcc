// # include <iostream>

// int main(){
//     auto result = (10 <=> 20) > 0; // "<=>" is called spaceship operator for testing compiler supports our given (in this case c++ 20) version or not.
//     std::cout << result << std::endl;
// }

// Basic Operations That We Can Do On Our Data:

    // add
    // subtract
    // multiply 
    // divide
    // modulus

        // Addition:

            // int number1 {2};
            // int number2 {7};

            // int sum {number1 + number2};
            // int other_sum = number1 + number2 + number1;

        // Subtraction:

            // -

        // Multiply:

            // *

        // Divide:

            // / // With integer division we're not going to be getting fractional numbers so if we'll do something like this 31/10 it will see how many times 10 is getting fit in 31 and then it'll maybe give the result 3 because we can't store fractional numbers in an integer normally...

        // Modulus:

            // % // 31%10 = 1 remainder // we maybe can't do this operation on fractional numbers and floating points.

