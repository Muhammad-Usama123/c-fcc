// # include <iostream>

// int main(){
//     auto result = (10 <=> 20) > 0; // "<=>" is called spaceship operator for testing compiler supports our given (in this case c++ 20) version or not.
//     std::cout << result << std::endl;
// }

// Ternary Operator:

    // This is an alternative way to do tests with the if, else statement...

    // Ternary Expression:

        // result = condition ? option1: option2; // option1 and option2 should be of the same type or convertable to same type otherwise compiler error...

        